<section class="hero">
	<div class="hero-overlay"></div>
	<div class="hero-content fade-in">
		<h1>AI-Powered recipe discovery made simple</h1>
		<div class="separator"></div>
		<p>Find the healthiest recipes on the web with a simple swipe — health-curated, diet-perfect, stress-free.</p>
		<div class="hero-buttons">
			<a href="#features" class="button-primary">Explore Features</a>
			<a href="#how-it-works" class="button-secondary">How It Works</a>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		min-height: 100vh;
		min-height: 100dvh;
		display: flex;
		align-items: center;
		justify-content: center;
		background: url('/images/hero-food-1.jpg') center/cover no-repeat;
		padding: 6rem 5% 4rem;
	}

	.hero-overlay {
		position: absolute;
		inset: 0;
		background: linear-gradient(
			180deg,
			rgba(0, 0, 0, 0.5) 0%,
			rgba(0, 0, 0, 0.55) 50%,
			rgba(0, 0, 0, 0.6) 100%
		);
	}

	.hero-content {
		position: relative;
		z-index: 1;
		text-align: center;
		max-width: 750px;
		color: var(--color-white);
	}

	h1 {
		font-size: 3.5rem;
		font-weight: 700;
		line-height: 1.15;
		margin-bottom: 1.5rem;
		letter-spacing: -0.02em;
	}

	.separator {
		width: 80px;
		height: 3px;
		background: var(--color-white);
		margin: 0 auto 1.5rem;
		border-radius: 2px;
		opacity: 0.7;
	}

	p {
		font-size: 1.25rem;
		line-height: 1.7;
		opacity: 0.9;
		margin-bottom: 2.5rem;
		font-weight: 400;
	}

	.hero-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.button-primary,
	.button-secondary {
		padding: 0.9rem 2.2rem;
		border-radius: var(--radius-pill);
		font-weight: 600;
		font-size: 1rem;
		transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease;
		cursor: pointer;
	}

	.button-primary {
		background: var(--color-primary-gradient);
		color: var(--color-white);
	}

	.button-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 30px rgba(74, 155, 107, 0.5);
	}

	.button-secondary {
		background: rgba(255, 255, 255, 0.15);
		color: var(--color-white);
		border: 2px solid rgba(255, 255, 255, 0.5);
		backdrop-filter: blur(4px);
	}

	.button-secondary:hover {
		background: rgba(255, 255, 255, 0.25);
		transform: translateY(-2px);
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 2.5rem;
		}
		p {
			font-size: 1.1rem;
		}
		.hero {
			min-height: 90vh;
			padding: 5rem 4% 3rem;
		}
	}

	@media (max-width: 480px) {
		h1 {
			font-size: 2rem;
		}
		p {
			font-size: 1rem;
		}
		.button-primary,
		.button-secondary {
			padding: 0.8rem 1.8rem;
			font-size: 0.9rem;
		}
	}
</style>
