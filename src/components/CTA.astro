<section class="cta-section">
	<div class="cta-content fade-in">
		<h2>Ready to transform your cookbook?</h2>
		<p>Download plaite to discover healthy recipes, curate the best possible cookbook, and share recipes your whole family will love.</p>
		<a href="https://apps.apple.com/us/app/plaite/id6738847946" class="cta-button" target="_blank" rel="noopener">Download the app now!</a>
	</div>
</section>

<style>
	.cta-section {
		padding: 6rem 5%;
		background: var(--color-primary-gradient);
		text-align: center;
	}

	.cta-content {
		max-width: 700px;
		margin: 0 auto;
	}

	h2 {
		font-size: 2.8rem;
		font-weight: 700;
		color: var(--color-white);
		margin-bottom: 1.25rem;
	}

	p {
		font-size: 1.15rem;
		color: rgba(255, 255, 255, 0.9);
		line-height: 1.7;
		margin-bottom: 2.5rem;
	}

	.cta-button {
		display: inline-block;
		background: var(--color-white);
		color: var(--color-primary);
		padding: 1rem 2.5rem;
		border-radius: var(--radius-pill);
		font-weight: 700;
		font-size: 1.1rem;
		transition: transform 0.3s ease, box-shadow 0.3s ease;
	}

	.cta-button:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
	}

	@media (max-width: 768px) {
		.cta-section {
			padding: 4rem 4%;
		}
		h2 {
			font-size: 2rem;
		}
		p {
			font-size: 1.05rem;
		}
	}

	@media (max-width: 480px) {
		h2 {
			font-size: 1.75rem;
		}
		.cta-button {
			padding: 0.85rem 2rem;
			font-size: 1rem;
		}
	}
</style>
